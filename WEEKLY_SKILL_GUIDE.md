# 周刊生成工具 - 使用指南

## 概述

这是一套完整的周刊生成工具，专门为 ryanwang's weekly 项目设计。工具可以从 JavaScript Weekly RSS 订阅源自动获取最新内容，并生成符合项目规范的周刊文件。

## 快速开始

### 方式一：直接使用 Python 脚本（推荐）

```bash
cd /Users/ryanwang/workspace/weekly
python3 scripts/generate_weekly.py
```

### 方式二：在 Claude Code 中使用

直接对 Claude 说：

```
请帮我生成新一期的周刊
```

或者：

```
使用 weekly skill 生成新一期
```

## 功能特性

- ✅ **自动获取最新内容**：从 JavaScript Weekly RSS 获取最新的 7-10 条新闻
- ✅ **智能期数计算**：自动分析现有周刊，计算下一期编号
- ✅ **路径自动生成**：根据当前日期自动创建正确的目录结构
- ✅ **格式化输出**：完全遵循项目的 Markdown 格式规范
- ✅ **关键词提取**：智能识别并提取技术关键词
- ✅ **美化列表**：确保列表项格式统一、美观（每个列表项单独一行，带换行）

## 执行示例

```bash
$ python3 scripts/generate_weekly.py

============================================================
周刊生成工具
============================================================

✓ 当前最新期数: 61
✓ 将生成第 62 期

正在获取 RSS 内容...
正在从 API 获取 RSS 内容: https://api.rss2json.com/v1/api.json?rss_url=...
✓ 成功获取 10 条新闻

正在生成周刊内容...

✓ 周刊文件已生成: docs/2026/01/62/index.md

提示: 请编辑文件，添加封面图片和优化内容

============================================================
完成！
============================================================
```

## 生成后的文件格式

```markdown
---
template: main.html
---

# ryanwang's weekly 第62期

![](https://to-out-use.oss-cn-hangzhou.aliyuncs.com/source/PLACEHOLDER.png?x-oss-process=image/auto-orient,1/interlace,1/quality,q_30/format,webp){ loading=lazy }

(封面图片来源于：待定)


------

!!! tip hint important "温馨提示"

    周刊中所有高亮的内容都可以点击到指定内容的链接～

---
> 如果您正处在微信公众号，请直接滚动至底部👉阅读原文🫶

---

**关键词：** *`JavaScript`*、*`Bun`*

## FE News

### 1.[jQuery 4.0 发布](https://javascriptweekly.com/issues/769)
> 相关地址：https://javascriptweekly.com/issues/769

jQuery 4.0 发布 — 在其首次发布 20 年后，这个流行的库迎来了 4.0 版本，迁移到 ES 模块...

### 2.[Can we ever fix the web dependency mess?](https://javascriptweekly.com/issues/768)
> 相关地址：https://javascriptweekly.com/issues/768

Web Dependencies are Broken; Can We Fix Them? — Lea 深入分析了 JavaScript 依赖管理的复杂性...

## End

!!! pied-piper "交个朋友吧～"

    我是不换(书生)，"浪子回头金不换"的**不换**，"百无一用是书生"的**书生**，热爱工作，同时在工作之余也热爱开源。

本期周刊到这里就结束了，我们下期再见👋～
```

## 后续处理步骤

生成文件后，建议手动进行以下优化：

### 1. 添加封面图片

编辑生成的文件（如 `docs/2026/01/62/index.md`）：

```markdown
![](https://to-out-use.oss-cn-hangzhou.aliyuncs.com/source/你的图片.png?x-oss-process=image/auto-orient,1/interlace,1/quality,q_30/format,webp){ loading=lazy }

(封面图片来源于：你的图片来源)
```

### 2. 优化新闻描述

- 检查自动提取的描述是否准确
- 补充重要的技术细节
- 添加特性列表（如果适用）：

```markdown
### 3.[Chrome 144 发布](链接)
> 相关地址：链接

Chrome 144 本周发布，带来了以下新特性：

- 正式支持 Temporal API
- 新增 CSS 特性
- 性能优化

这是一个重要的里程碑。
```

### 3. 调整关键词

检查自动提取的关键词，确保准确反映本期重点：

```markdown
**关键词：** *`Temporal API`*、 *`React 19.2`*、*`Node.js Security`*
```

### 4. 格式美化

- 确保列表项使用 `- ` 格式
- 每个列表项单独一行
- 列表项之间保持适当间距
- 确保所有链接可点击

## 工作原理

### RSS 数据源

使用 rss2json API 获取 JavaScript Weekly 内容：

```
https://api.rss2json.com/v1/api.json?rss_url=https://javascriptweekly.com/rss/
```

**为什么使用 API？**
- 避免 CORS 问题
- 直接返回 JSON 格式，更易解析
- 稳定可靠

### 期数计算

脚本扫描 `docs/` 目录，找到所有已存在的期数，取最大值 +1：

```python
# 示例
docs/2026/01/61/  # 最大期数是 61
→ 下一期是 62
```

### 关键词提取

从新闻标题中智能匹配预定义的技术关键词列表，包括：

**框架/库：**
React, Vue, Angular, Next.js, Nuxt, Svelte, Astro

**运行时：**
Node.js, Bun, Deno

**工具：**
Vite, Webpack, ESLint, TypeScript, Prettier

**浏览器：**
Chrome, Firefox, Safari, Edge

**标准：**
TC39, ECMAScript, WebAssembly, CSS

**其他：**
Security, Performance, Accessibility, API, Testing

## 自定义配置

### 添加更多关键词

编辑 [`scripts/generate_weekly.py`](scripts/generate_weekly.py)，找到 `common_keywords` 列表：

```python
common_keywords = [
    'React', 'Vue', 'Angular',
    'YourCustomKeyword',  # 添加你的关键词
    # ...
]
```

### 更改 RSS 源

修改 `main()` 函数中的 `rss_url`：

```python
# 支持 Cooper Press 的所有周刊
rss_url = "https://nodeweekly.com/rss/"
# 或
rss_url = "https://css-weekly.com/rss/"
# 或
rss_url = "https://react.statuscode.com/rss"
```

### 调整新闻数量

修改 `generate_weekly_content()` 函数：

```python
news_items = []
for i, item in enumerate(items[:15], 1):  # 改为 15 条
    news_items.append(format_news_item(i, item))
```

## 项目文件结构

```
weekly/
├── .claude/
│   └── skills/
│       └── weekly.md              # Skill 定义文档
├── scripts/
│   ├── generate_weekly.py         # 主脚本
│   └── README.md                  # 脚本使用说明
├── docs/
│   ├── 2022/
│   ├── 2023/
│   ├── 2024/
│   ├── 2025/
│   └── 2026/
│       └── 01/
│           └── 61/
│               └── index.md       # 现有周刊
└── WEEKLY_SKILL_GUIDE.md          # 本文档
```

## 故障排除

### 问题 1：网络请求失败

**错误信息：**
```
✗ 错误: 网络请求失败: ...
```

**解决方案：**

1. 检查网络连接
2. 测试 API 可访问性：
   ```bash
   curl "https://api.rss2json.com/v1/api.json?rss_url=https://javascriptweekly.com/rss/"
   ```
3. 检查是否需要配置代理

### 问题 2：生成的新闻数量不足

**可能原因：**
- RSS 源更新较慢
- API 返回的数据被过滤

**解决方案：**
1. 手动访问 RSS 源确认是否有新内容
2. 检查脚本的过滤逻辑
3. 稍后重试

### 问题 3：描述内容不完整

**解决方案：**
手动编辑生成的文件，补充完整的新闻描述

### 问题 4：关键词提取不准确

**解决方案：**

1. 编辑 `scripts/generate_weekly.py` 中的 `common_keywords` 列表
2. 手动修改生成文件中的关键词部分

## 技术栈

- **Python 3.6+**
- **标准库**：
  - `json` - JSON 解析
  - `urllib` - HTTP 请求
  - `re` - 正则表达式
  - `pathlib` - 路径操作
  - `datetime` - 日期处理
- **外部服务**：
  - rss2json.com - RSS 转 JSON API

## 扩展建议

### 1. 支持多个 RSS 源

```python
def fetch_from_multiple_sources():
    sources = [
        'https://javascriptweekly.com/rss/',
        'https://nodeweekly.com/rss/',
        'https://css-weekly.com/rss/',
        'https://frontendfoc.us/rss'
    ]

    all_items = []
    for source in sources:
        try:
            items = fetch_rss_from_api(source)
            all_items.extend(items)
        except Exception as e:
            print(f"警告: 无法获取 {source}: {e}")

    # 按日期排序并去重
    all_items = sort_and_deduplicate(all_items)
    return all_items[:10]  # 只取前 10 条
```

### 2. 自动添加封面图片

```python
# 使用随机图片 API
def get_random_cover_image():
    import random
    images = [
        "https://source.unsplash.com/random/800x600/?technology",
        # 添加更多图片来源
    ]
    return random.choice(images)
```

### 3. 集成 AI 摘要

使用 Claude API 自动生成更优质的新闻摘要。

## 常见问题 (FAQ)

**Q：生成的文件可以直接使用吗？**

A：生成了基本框架，但建议手动添加封面图片和优化内容描述。

**Q：支持哪些 RSS 源？**

A：理论上支持任何标准 RSS 格式，已测试 JavaScript Weekly、Node Weekly 等。

**Q：如何修改模板格式？**

A：编辑 `scripts/generate_weekly.py` 中的 `generate_weekly_content()` 函数。

**Q：可以批量生成多期吗？**

A：当前脚本每次生成一期，如需批量生成，请自行修改脚本。

**Q：生成的文件编码是什么？**

A：UTF-8，Unix 风格换行符（\n）。

## 贡献

如有改进建议或发现问题，欢迎：

1. 修改脚本并测试
2. 更新相关文档
3. 提交 Issue 或 PR

## 更新日志

### v1.0.0 (2025-01-24)

- ✅ 初始版本发布
- ✅ 支持从 JavaScript Weekly RSS 获取内容
- ✅ 自动期数计算和路径生成
- ✅ 智能关键词提取
- ✅ 格式化输出

## 许可

本工具遵循项目主许可协议。

---

**作者备注：** 这个工具旨在节省周刊编写时间，自动化的同时保持了内容的灵活性。生成的内容总是需要人工审核和优化，以确保质量。
